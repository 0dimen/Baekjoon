#include <iostream>
#include <cmath>
#include <list>
using namespace std;

int main(){
    int N;
    cin >> N;
    int n = (int)log10(N);
    list<int> num(10);
    for(int i = 0; i<n; i++){
        
    }

    return 0;
}