#include <iostream>

using namespace std;

int main(){
    int N,M,R;
    vector<int> A;
    vector<int> B;
    
    for (int i =0; i<N; i++){
        int input;
        cin >> input;
        A.push_back(input);
    }
    for (int i = 0; i<M; i++){
        int input;
        cin >> input;
        B.push_back(input);
    }

    sort(A.begin(), A.end());
    sort(B.begin(), B.end());

    for()

    
}